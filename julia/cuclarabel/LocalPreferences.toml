[CUDA_Runtime_jll]
# Use the CUDA 13 artifact runtime so CUDSS_jll can select its matching artifact.
# The local CUDA toolkit does not include the separately distributed cuDSS library.
local = "false"
version = "13.0"
